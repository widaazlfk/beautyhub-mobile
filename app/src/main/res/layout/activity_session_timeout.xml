<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    android:padding="32dp"
    android:gravity="center">

    <!-- Warning Icon -->
    <ImageView
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@drawable/ic_session_timeout"
        android:tint="@color/orange"
        android:layout_marginBottom="24dp" />

    <!-- Title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Session Timeout"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- Countdown Timer -->
    <TextView
        android:id="@+id/tv_countdown"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Auto logout in 00:30"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/orange"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- Message -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your session is about to expire due to inactivity. Would you like to stay signed in?"
        android:textSize="16sp"
        android:lineSpacingExtra="6dp"
        android:gravity="center"
        android:textColor="@color/gray"
        android:layout_marginBottom="32dp" />

    <!-- Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center">

        <Button
            android:id="@+id/btn_stay_signed_in"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Stay Signed In"
            android:textSize="16sp"
            android:textStyle="bold"
            android:backgroundTint="@color/primary_color"
            style="@style/Widget.Material3.Button"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/btn_logout_now"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Log Out Now"
            android:textSize="16sp"
            android:backgroundTint="@color/transparent"
            android:textColor="@color/gray"
            style="@style/Widget.Material3.Button.TextButton" />

    </LinearLayout>

</LinearLayout>