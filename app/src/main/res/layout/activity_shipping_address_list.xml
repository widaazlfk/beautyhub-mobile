<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".buyer.ShippingAddressListActivity">

    <!-- AppBar Layout untuk memegang Toolbar -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar_shipping_address"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:layout_constraintTop_toTopOf="parent"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:navigationIconTint="@color/white"
            app:title="Shipping Address"
            app:titleTextColor="@color/white" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- FrameLayout untuk menguruskan RecyclerView dan mesej kosong -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- RecyclerView untuk memaparkan senarai alamat -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view_addresses"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:padding="8dp"
            tools:listitem="@layout/item_shipping_address" /> <!-- Guna 'item_shipping_address.xml' anda untuk pratonton -->

        <!-- Mesej untuk dipaparkan apabila tiada alamat -->
        <TextView
            android:id="@+id/tv_no_addresses"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/no_shipping_addresses_found"
            android:textAppearance="?attr/textAppearanceBodyLarge"
            android:textColor="@color/gray"
            android:visibility="gone"
            tools:visibility="visible" />

    </FrameLayout>

    <!-- Floating Action Button (FAB) untuk menambah alamat baharu -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_address"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:contentDescription="@string/add_new_address"
        app:srcCompat="@drawable/ic_add"
        app:tint="?attr/colorOnPrimary"
        android:backgroundTint="?attr/colorPrimary" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
